import React, { useState } from "react";
import "./join.css";

const Join = () => {
    const [openMenu, setOpenMenu] = useState(false);
  
    return (
        <div>
            <h1>Hello!</h1>
        </div>
    );
}

export default Join;
